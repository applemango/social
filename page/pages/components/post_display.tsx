import Image from "next/image";

import { getUrl } from "../../lib/main"

import styles from "./styles/post_display.module.scss"

type Props = {
}
const Post_image = ({  }:Props) => {
    return (
        <div className = { styles.main }>
        </div>
    )
}
export default Post_image